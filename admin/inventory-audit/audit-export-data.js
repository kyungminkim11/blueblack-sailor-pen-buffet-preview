import{state,productKey,dateText}from'./audit-state.js';
export function makeExportData(){const records=Object.values(state.counts),areas=[...new Set(records.map(row=>row.area))],unknowns=Object.values(state.unknowns);const summary=[['블루블랙 펜샵 재고 조사 결과'],[],['조사 시작',dateText(state.startedAt)],['마지막 저장',dateText(state.updatedAt)],['조사 구역 수',areas.length],['조사 상품 종류',new Set(records.map(productKey)).size],['조사 총수량',records.reduce((sum,row)=>sum+Number(row.qty||0),0)],['미등록 바코드',unknowns.reduce((sum,row)=>sum+Number(row.count||0),0)],[],['구역','상품 종류','조사 수량']];areas.forEach(area=>{const rows=records.filter(row=>row.area===area);summary.push([area,new Set(rows.map(productKey)).size,rows.reduce((sum,row)=>sum+Number(row.qty||0),0)])});
const detail=[['조사 구역','품목코드','바코드','제조사','상품명','매장 판매가','이카운트 재고','조사 수량','조사 방식','최종 수정']];records.sort((a,b)=>a.area.localeCompare(b.area,'ko')||a.product_name.localeCompare(b.product_name,'ko')).forEach(row=>detail.push([row.area,String(row.item_code||''),String(row.barcode||''),row.manufacturer||'',row.product_name||'',Number(row.consumer_price)||'',Number(row.stock_qty)||0,Number(row.qty)||0,row.source||'',dateText(row.lastAt)]));
const grouped=new Map();records.forEach(row=>{const key=productKey(row),item=grouped.get(key)||{...row,total:0,areas:new Set()};item.total+=Number(row.qty||0);item.areas.add(row.area);grouped.set(key,item)});const products=[['품목코드','바코드','제조사','상품명','매장 판매가','이카운트 재고','조사 수량','차이','조사 구역']];[...grouped.values()].sort((a,b)=>a.product_name.localeCompare(b.product_name,'ko')).forEach(row=>products.push([String(row.item_code||''),String(row.barcode||''),row.manufacturer||'',row.product_name||'',Number(row.consumer_price)||'',Number(row.stock_qty)||0,row.total,'',[...row.areas].join(', ')]));
const unknown=[['조사 구역','미등록 바코드','발생 횟수','최초 스캔','마지막 스캔']];unknowns.forEach(row=>unknown.push([row.area,String(row.barcode),Number(row.count),dateText(row.firstAt),dateText(row.lastAt)]));return{records,summary,detail,products,unknown}}
