name: Legacy ink color crawler disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Inventory CSV is authoritative
        run: echo "Decant ink colors are managed from the verified inventory CSV and are not auto-crawled from the full store catalog."
